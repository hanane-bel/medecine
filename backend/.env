# Replace 'your_password' with the password you set during PostgreSQL installation
# You will need to create a database named 'med_db' using pgAdmin or the psql shell
DATABASE_URL=postgres://postgres:khalil@localhost:5432/med_db
